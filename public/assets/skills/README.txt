Drop screenshots in here named after each skill's id, e.g.:

  hiking-planner.png
  cybersec-podcast.png
  future-research-institute.png
  ...

The filename must match the "id" field of the matching product entry in
locales/en.json / locales/zh.json (same id, both files). If a matching
file isn't found, the site shows a placeholder box automatically — no
other code changes needed once you drop the image in.
